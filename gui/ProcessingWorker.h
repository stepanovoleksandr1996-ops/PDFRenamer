#ifndef PROCESSINGWORKER_H
#define PROCESSINGWORKER_H

#include <QObject>
#include <string>

//======================================================
// ProcessingWorker
//
// Окремий клас, який запускає ядро програми
// у робочому потоці.
//
// Сам клас НЕ містить логіки обробки PDF.
// Він лише:
//
// • приймає шляхи;
// • викликає processFolder();
// • повідомляє GUI про перебіг роботи.
//
//======================================================

class ProcessingWorker : public QObject
{
    Q_OBJECT

public:

    explicit ProcessingWorker(QObject *parent = nullptr);

    //--------------------------------------------------
    // Передати параметри запуску.
    //--------------------------------------------------

    void setPaths(
        const std::string& folder,
        const std::string& excel);

public slots:

    //--------------------------------------------------
    // Почати обробку.
    //--------------------------------------------------

    void process();

signals:

    //--------------------------------------------------
    // Додати повідомлення у журнал.
    //--------------------------------------------------

    void logMessage(
        const QString& text);

    //--------------------------------------------------
    // Оновити прогрес.
    //
    // value = 0..100
    //--------------------------------------------------

    void progressChanged(
        int value);

    //--------------------------------------------------
    // Обробка завершена.
    //--------------------------------------------------

    void finished();

private:

    std::string folderPath;

    std::string excelFile;
};

#endif